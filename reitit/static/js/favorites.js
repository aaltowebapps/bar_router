// Generated by CoffeeScript 1.3.1
var Favorites;

Favorites = {
  add: function(address) {
    var favLocations;
    favLocations = this.getFavoriteLocations();
    if (favLocations.indexOf(address) === -1) {
      favLocations.push(address);
      this.setFavoriteLocations(favLocations);
    }
  },
  remove: function(address) {
    var favLocations, index;
    favLocations = this.getFavoriteLocations();
    index = favLocations.indexOf(address);
    if (index !== -1) {
      favLocations.splice(index(1));
      this.setFavoriteLocations(favLocations);
    }
  },
  getFavoriteLocations: function() {
    var flatList, result, store;
    result = new Array();
    store = window.localStorage;
    if (store !== null) {
      flatList = store.getItem("favLocations");
      if (flatList !== null) {
        result = flatList.split(" /||/ ");
      }
    }
    return result.clean("");
  },
  setFavoriteLocations: function(locationArray) {
    var flatList, store;
    if (locationArray !== null && locationArray.length !== 0) {
      flatList = locationArray.join(" /||/ ");
      store = window.localStorage;
      if (store !== null) {
        store.setItem("favLocations", flatList);
      }
    }
  }
};
