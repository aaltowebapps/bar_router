requirements:

requirements.txt is a genereated file. All requirements can be installed with:

pip install -r requirements.txt


You need to get pip first.

On ubuntu:
sudo apt-get install python-setuptools
suod easy_install pip



Coffee Script
	Highlighting in gedit:	https://github.com/wavded/gedit-coffeescrip
	Get the thing:		sudo apt-get install coffeescript
	Compile the code:	coffee -c js/
